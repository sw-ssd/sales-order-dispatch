# Pixso 版面美化 — 進度存檔

> **狀態**：第一輪（版面修正＋視覺補強）與**第二輪（留白補強，見 §8）**皆已完成並在 Pixso 文件內生效（Pixso 會即時持久化，無需另外「儲存」）
> **存檔日期**：2026-09-18（第二輪同日追加）
> **設計檔**：`多公司訂出貨系統` UI 稿
> **file key**：`lzruuV4902NTkKLXhCM71w`　**page id**：`0:1`（页面 1）
> **開啟連結**：https://pixso.net/app/design/lzruuV4902NTkKLXhCM71w?icon_type=1&page-id=0%3A1
> **原始需求**：使用者一句「美化 Pixso 版面」（未指定範圍與方向，後續指示「無須截圖確認」）

---

## 1. 本輪範圍與原則

使用者未指定範圍，採以下預設並一次做完：

- **範圍**：全檔 15 個畫面（Web 9 + App 6）
- **幅度**：可重排區塊、間距、元件尺寸；**不新增功能模組**；文案只做必要修正（移除破字 emoji、補測試資料）
- **風格**：維持原有藍色商務風（Inter + slate/blue 色票 + 既有圓角/間距節奏），只做精緻化，不換設計語言
- **不在範圍**：不動程式碼；不改資訊架構；不新增畫面

---

## 2. 檔案現況

### 2.1 畫面清單與畫布座標（本輪已重新排列）

先前 15 個畫面**全部疊在 (0,0)**，現為 Web 3×3 網格 + App 單列。

| 畫面 | 節點 id | 畫布座標 | 尺寸 |
|---|---|---|---|
| Web-登入 | `3:60` | (0,0) | 1440×900 |
| Web-Dashboard | `3:99` | (1560,0) | 1440×900 |
| Web-客戶總表 | `3:237` | (3120,0) | 1440×900 |
| Web-商品總表 | `3:454` | (0,1040) | 1440×900 |
| Web-訂單管理 | `3:590` | (1560,1040) | 1440×900 |
| Web-派車規劃 | `3:734` | (3120,1040) | 1440×900 |
| Web-單據列印 | `3:892` | (0,2080) | 1440×900 |
| Web-管理人員名單 | `3:992` | (1560,2080) | 1440×900 |
| Web-角色權限 | `3:1129` | (3120,2080) | 1440×900 |
| App-身分選擇 | `3:1256` | (0,3260) | 390×844 |
| App-店家登入 | `3:1283` | (470,3260) | 390×844 |
| App-首頁 | `3:1321` | (940,3260) | 390×844 |
| App-商品下單 | `3:1396` | (1410,3260) | 390×844 |
| App-訂單歷史 | `3:1439` | (1880,3260) | 390×844 |
| App-功能 | `3:1495` | (2350,3260) | 390×844 |

排列規則：Web 水平間距 1560、垂直間距 1040；App 水平間距 470、整列位於 y=3260。

### 2.2 全檔統計

- 節點 1,948 個、文字節點 539 個、裁切容器 40 個
- 設計語言（沿用未改）：Inter；`#0F172A / #334155 / #64748B / #94A3B8 / #2563EB / #1D4ED8 / #F8FAFC / #F1F5F9 / #EFF6FF` + 狀態色 `#DCFCE7+#15803D`（成功）、`#FEF3C7+#B45309`（等待）、`#FEF2F2+#DC2626`（停用）；圓角 8/12/14/16/999；間距 4/8/12/16/20/24

---

## 3. 本輪改動明細

### 3.1 修掉的版面錯誤（有實際裁切／破損）

| 畫面 | 問題 | 處理 | 關鍵節點 |
|---|---|---|---|
| Web-登入 | 表單欄寬 166px（卡片內應 336）、密碼欄僅 28px、眼睛圖示錯位、標題框 440px 溢出 360px 容器 | 表單／欄位／按鈕／SSO 全寬 336 對齊；標題與描述框改 360 + 自動高 | `3:75`(Form) `3:78` `3:82` `3:83` `3:66` `3:67` |
| Web-客戶總表 | 抽屜硬疊表格、無遮罩；付款方式／車次欄 52px / 26px，文字溢出框外 | 加 `Scrim`（1440×900、#0F172A 40% opacity、位於抽屜之上之前）；兩欄改 hug → 100px | `3:1588`(Scrim) `3:422` `3:430` |
| Web 四張資料表 | 卡片僅 204px、分頁浮在卡片中間，下方大片灰底 | 卡片撐滿內容區 728px，新增 `Spacer-Tail` 彈性間距器讓 `Pagination` 固定貼底 | 客戶 `3:322`/`3:1584`/`3:387`、商品 `3:533`/`3:1585`/`3:583`、訂單 `3:667`/`3:1586`/`3:727`、人員 `3:1066`/`3:1632`/`3:1625` |
| Web-管理人員名單 | 缺分頁列（與其他表不一致） | 從訂單管理複製分頁列補上（顯示 1–10，共 24 筆） | `3:1625` |
| App-首頁 | 訂單卡片 Body 寬度只有 **1px** → 品名被切；底部導覽 4 項全擠左側；Banner 內容擠頂部、標題 emoji 顯示為破字 | Body/Row 改 STRETCH → 318px；導覽 4 項各 98px 平均分佈；Banner 內容垂直居中；emoji 移除 | `3:1353` `3:1354` `3:1367` `3:1333` `3:1334` |
| App-店家登入 | 密碼欄 28px 破損；Email 與密碼欄寬度不一致 | 兩欄皆 334px；Body 內容垂直居中 | `3:1292`(Form) `3:1293` `3:1297` `3:1295` `3:1299` |
| App-商品下單 | 浮動列右移 20px（`x=20`）、懸在清單中段（`y=736`） | `x=0`、`y=792`（貼齊 844 底部） | `3:1437` |
| App-功能 | 本輪補的項目造成「關於我們」重複 | 移除重複項，回復 5 項（帳號管理／我的 QR Code｜設定／關於我們／隱私權政策） | `3:1495` |

### 3.2 視覺補強（原本空蕩或不平衡）

| 畫面 | 改動 |
|---|---|
| Web-Dashboard | 最近訂單卡片 220 → 638px（撐滿內容區）；資料列 2 → 8；金額欄由 `—` 補為實際數字；狀態標籤三色（待派車／已確認／已派車） → `3:191` `3:195` |
| Web-客戶總表／商品總表／訂單管理／管理人員名單 | 資料列 2 → 5（依各表欄位補一致的測試資料與狀態色；客戶表含一筆「停用」紅色標籤） |
| Web-角色權限 | 權限矩陣 4 → 7 列（列印單據／匯出報表／系統設定） |
| App-身分選擇 | Body 內容垂直居中（原本整塊偏上、下方空 380px） |
| App-訂單歷史 | 訂單卡 2 → 5（含已完成／已出貨／退貨中三種狀態色） |
| App-商品下單 | 商品清單 3 → 8 項 |
| App-首頁／商品下單／訂單歷史／功能 | 捲動區設 `clipsContent`，確保內容超出捲動範圍時被裁切而非溢出畫板 |

---

## 4. 驗證結果

- 15 個畫面全部匯出 PNG 逐一檢視（本輪檢視 13 張；派車規劃、單據列印未改動故未檢視）：無裁切、無重疊、無破損尺寸
- 全檔自動掃描「子節點溢出容器」與「零尺寸容器」：已無 >8px 的可見溢出（僅剩下列微瑕）
- 表格改動後重新讀回驗證：卡片高度 728、資料列數 5、分頁位於卡片最底部

### 已知微瑕（不影響顯示，刻意保留未動）

1. `Web-Dashboard > C`：訂單編號文字框（96px）比儲存格（90px）寬 9px → 溢位落在 16px 欄距內，畫面無裁切
2. `App-商品下單 > Scroll`：捲動區內容 789px / 可用 748px → 超出約 41px，已由裁切處理（等同真實可捲動清單）
3. 資料表狀態標籤（27px）比列高（24px）高 3px → 落在列的下緣 padding 內

---

## 5. 尚未處理的留白（設計取捨，非錯誤）

第一輪刻意不動的留白，其中三處已於**第二輪（見 §8）**補強；下列為補強後狀態：

| 位置 | 留白 | 狀態 |
|---|---|---|
| Web-派車規劃 看板欄位 Col-1/2/3車 | 原 464 / 464 / 514px | ✅ 第二輪補卡片＋底部小計，僅剩彈性間距 |
| Web-單據列印 Paper | 原 470px | ✅ 第二輪補至 632/654px（5 組、10 品項、頁尾） |
| Web-角色權限 RoleList | 原 534px | ✅ 第二輪補至 8 個內建角色 |
| Web 各頁 Sidebar | 438 ~ 566px | 導覽列，項目數固定（未動） |
| Web-登入 LeftBrand / RightForm | 667 / 386px | 深色品牌區與卡片置中的既有留白 |
| App-首頁 / 訂單歷史 / 功能 | 底部 175 / ~100 / 365px | 行動版清單底部留白，屬正常 |
## 6. 待決事項（等指示再動）

1. ~~派車規劃、單據列印是否補內容~~ → **已於第二輪完成**（見 §8）
2. **是否匯出 15 張快照存檔**：尚未執行；如需可一次匯出至 `docs/design/snapshots/`（1440×900 JPEG，約 4–5 MB）
3. ~~本文件是否納入 git~~ → **已納入 git**（僅提交 `docs/design/`，未觸碰他人 `docs/superpowers/**` 未提交變更）

---

## 7. 接手／重做指引

### 7.1 回退方式

本輪全部改動都在 **Pixso 文件內**，沒有任何程式碼檔案被修改。回退請用 Pixso 的復原（⌘Z）或設計檔版本歷史，勿從本文件反推重建。

### 7.2 工具與環境

- 使用 Pixso 桌面版 MCP：`eval_script`（Pixso Plugin API）為主，逐項「讀取 → 寫入 → 讀回驗證」
- 未使用 `apply_design` DSL 路線；僅在需要新節點時用 `pixso.createFrame()` + `insertChild`

### 7.3 實作時踩到的 Pixso API 限制（重要）

1. **autolayout 子節點的 `width` / `height` 直接賦值無效**，要用：
   - 交叉軸伸縮：`node.layoutAlign = 'STRETCH'`
   - 主軸撐滿：`node.layoutGrow = 1` 或 `node.layoutSizingHorizontal/Vertical = 'FILL'`
   - 文字框換行：`textAutoResize = 'HEIGHT'`（固定寬自動高）
2. **`createFrame()` 建立的節點必須先 `insertChild()` 進父層**，才能設 `layoutPositioning = 'ABSOLUTE'` 或 `layoutGrow`（先設會丟錯／被忽略）
3. **要插在特定位置**用 `parent.insertChild(index, node)`；尾巴留白用空 frame + `layoutGrow = 1`（`fills = []`）
4. **複製既有列**：`row.clone()` → `parent.appendChild/insertChild` → 以 `collect(clone, n => n.type === 'TEXT')` 依序改 `characters`；注意各表欄位索引不同（客戶表首欄是勾選框，訂單表沒有）
5. 匯出檢視：`get_export_image`（得到 localhost 暫存 URL）→ `curl -o` 下載 → `view_images` 檢視；`take_screenshot` 的 base64 不易在對話中檢視

---

## 8. 第二輪美化：留白補強（2026-09-18 追加）

依第一輪 §5／§6 標示的三處留白補強，未觸碰其他畫面。

### 8.1 Web-派車規劃（`3:734`）

| 項目 | 內容 | 節點 id |
|---|---|---|
| 1車 補 2 張卡 | 永和豆漿（SO-2026-0011 · 6 項，待處理）、八方雲集 板橋店（SO-2026-0012 · 4 項，已處理） | `3:2032` `3:2047` |
| 2車 補 1 張卡 | 美而美早餐（SO-2026-0013 · 3 項，缺貨待確認） | `3:2066` |
| 3車 補 2 張卡 | 金滿堂婚宴會館（SO-2026-0014 · 5 項）、好味道食堂（SO-2026-0015 · 4 項） | `3:2085` `3:2100` |
| 欄底小計 | 彈性間距 Spacer（`fill_container`）＋ `ColFooter`，把「N 家客戶 / 合計 N 項」固定壓在欄底 | 1車 `3:2062`/`3:2063`、2車 `3:2081`/`3:2082`、3車 `3:2115`/`3:2116` |
| 3車拖曳區順序 | 虛線 `Empty` 移到兩張卡片下方、Spacer 之前 | `3:890` |

補強後卡片數與欄頭數字一致（1車 4 / 2車 3 / 3車 2）。

### 8.2 Web-單據列印（`3:892` → Paper `3:971`）

- 原有 2 組各 1 品項 → **5 組 10 品項**：
  - 1車·1F倉：里肌肉（薄切）×12 公斤、排骨（切塊）×3 公斤
  - 1車·2F倉：五花肉（厚切）×8 公斤、絞肉 ×5 公斤
  - 2車·1F倉：雞胸肉（切塊）×20 包、雞腿（去骨）×10 包
  - 2車·2F倉：培根 ×4 公斤
  - 3車·1F倉：香腸 ×6 包
- 新增 3 組：`3:2127`（2車·1F倉）、`3:2139`（2車·2F倉）、`3:2147`（3車·1F倉）
- 新增頁尾 `PFoot` `3:2155`：列印時間／列印人員＋「揀貨簽收：____」
- 內容高度 632 / 可用 654 → 紙面飽滿、無裁切

### 8.3 Web-角色權限（`3:1129` → RoleList `3:1180`）

角色清單 3 → **8 個**，依階層排序：

| 順序 | 角色 | 節點 id |
|---|---|---|
| 1 | 總管理員 | `3:2158` |
| 2 | 公司管理員 | `3:2161` |
| 3 | 部門管理員（選取中，對應矩陣標題） | `3:1182` |
| 4 | 一般職員 | `3:1185` |
| 5 | 會計（兼） | `3:1188` |
| 6 | 客戶（店家） | `3:2164` |
| 7 | 訪客 | `3:2167` |
| 8 | 開發者（逃生門） | `3:2170` |

### 8.4 驗證

- `check_layout` 逐區讀回：三處皆無溢出、無裁切、順序正確
- `get_export_image` 匯出 JPEG（1440×900）→ `curl` → `view_images` 目視確認三張畫面

### 8.5 過程中修正的失誤（備查）

- `M(node, parent, index)` 的 index 為 **1-based**；一度誤把 `rlGuestD`(`3:2169`)、`rlDevD`(`3:2172`) 當成角色框移動，已即時移回原角色框並改動正確節點（`3:2158` `3:2161`）
- 同一 parent 內移動時索引語意與直覺不同，已改用「移動目標節點本身 → 立即讀回確認」逐次校正

---

## 9. 第三輪：Dashboard 追上實作（2026-09-23）

`docs/design` 的 15 個原始畫面與本輪 20×2 的實作對齊稿都停在**首頁佔位**——首頁原為
「Wave 1 骨架」佔位（只有標題與一行說明），規格 §8.2 要求的今日待出貨、待處理訂單數量、
快速連結後來才實作（`d56fb5f`）。設計稿與實作分歧，故重出。

- **作法**：真瀏覽器（1440×900）+ 真 Postgres（`app_rw`）+ 示範租戶 → 逐主題擷取自足 HTML
  → `code_to_design` → 原地替換舊框（先移新框到定位、改名，再刪舊框，避免位置空窗）。
- **節點**：`7:1`（Light，1560,0）、`7:296`（Dark，1560,5200），名稱沿用
  `02-首頁Dashboard｜Light/Dark`，舊框 `6:14123`／`6:21172` 已刪。
- **驗證**：兩張截圖目視（`3／3／1` 三張數字卡、六個快速連結、兩個主題皆可讀）；
  DOM 斷言與 DB 逐筆相符（今日 pending 2 + processing 1 = 3、全公司 pending 3、processing 1）。
- 全檔仍為 **40 個 frame**（20 頁 × 2 主題），數量不變。

**揭露的兩個真實缺陷**（皆非設計稿問題，見 commit `ebc1fcd`／`83321c2`）：列印下載 URL 有兩個
產生端（回應整數 id、DB 檔名），以及示範 seeder 只建 `file_assets` 列、不落檔
（列印頁 6 條連結全 404）。兩者都只在「真的去點」時才看得到。
